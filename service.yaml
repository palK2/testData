apiVersion: v1
kind: Service
metadata:
  name: capstone-service   # 👈 MUST match existing service
spec:
  type: NodePort
  selector:
    app: capstone
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30008
